// map = means this is used to give each value of the list par yeh newArr bana deta hai means forEach bhi ek ek element deta tha but newArr nahi banata tha yeh doosra arr banane mai help krta hai 


let arr = [21,23,43,23];

let newArr = arr.map((val) => {
    return val * 2
});

console.log(newArr);


// isne newArr means alag hi file bana di and usme woh values ko store kr dia ek ek value ko multiply by 2 kr dia and store kr dia 


